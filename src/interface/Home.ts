export interface Home {
  _id: string;
  address: string;
  floorsNumber: number;
  description: string;
  label: string;
}

export interface NewHome {
  address: string;
  floorsNumber: number;
  description: string;
  label: string;
}
